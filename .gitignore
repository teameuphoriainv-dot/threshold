.DS_Store
node_modules/
.claude-flow/

ruvector.db
server/node_modules/

# launchd warden runtime logs
warden/*.log

# stray agent/runtime artifacts
stdb/spacetimedb/agentdb.rvf
stdb/spacetimedb/agentdb.rvf.lock
web/.vite/

# agentdb / ruflo runtime artifacts (anywhere)
agentdb.rvf
agentdb.rvf.lock
**/agentdb.rvf
**/agentdb.rvf.lock

# scratch dirs / build logs
tmp/
design/

# warden demo telemetry log (local only)
warden/reveal.jsonl
